# Template registry

The authoritative supplied workbook sources remain under `template/`. Their paths and SHA-256 hashes are recorded in `data/registered/schema_registry.json`; they are never overwritten. Future output-template files belong here only after their role and hash are registered.
